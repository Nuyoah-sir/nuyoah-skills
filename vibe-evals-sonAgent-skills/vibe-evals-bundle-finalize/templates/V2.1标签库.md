# V2.1 Vibe Tags 标签库（Coding Agent 人评标准 V 2.1 第五节全量）

> 用途：vibe-evals-vibe-form Phase 2 预选候选标签的**唯一合法标签集**；自定义标签仅在库中无匹配时由人工提出。
> 纪律：标签有依据就都选（可多选），只选有突出依据的，没有不硬贴；指纹=相对独特偏好，全员都用不算指纹；Pros 和 Cons 可以同时贴看似矛盾的标签，但须在备注说清楚。

## Pros

### Pros · 架构与组织

| tag | 定义 | 判定信号 |
| --- | --- | --- |
| 模块拆分清晰 | 多文件项目里能感受到职责划分明显，不是大杂烩 | 看产物文件树 / 模块名是否自解释 |
| 命名一致 | 全程同一种命名风格，不混用 camelCase / snake_case | 文件、变量等可见的取名 |

### Pros · 工程纪律

| tag | 定义 | 判定信号 |
| --- | --- | --- |
| 单文件也整洁 | 单 HTML / 单文件交付里，函数/类切分得当 | 浏览器 view-source 大致扫一眼粒度（限单文件题） |
| 错误处理完备 | 边界 case（空输入、网络失败、非法值）都做了 friendly 处理 | 故意输入空值、超长字符、断网模拟看反馈是否友好 |
| 操作流畅 | 大量数据 / 复杂动画 / 频繁交互下肉眼无明显卡顿、掉帧 | 实际拖拽、点击、输入若干次，凭手感判断；不强制开 DevTools Performance |

### Pros · 视觉与体验

| tag | 定义 | 判定信号 |
| --- | --- | --- |
| 审美在线 | 配色、间距、字体、留白形成统一风格，主题贴合度也算在内 | 整体看 30 秒就能感受到设计感；如题目写明主题（赛博朋克 / 工业风 / 节日主题），看 UI 是否真的呼应 |
| 微交互细腻 | 按压反馈、hover 状态、loading 占位都做了 | 鼠标过元素 / 点按钮 / 等异步看到细节 |
| 动效有节奏 | 动画时长、缓动、触发时机恰到好处 | 触发若干动画看是否自然不生硬 |
| 窗口自适应 | 不同屏幕尺寸下都不破相 | 浏览器拉窗口 / 切换设备模拟器，至少看 375 / 768 / 1920 三档 |

### Pros · 超预期交付

| tag | 定义 | 判定信号 |
| --- | --- | --- |
| 超预期交付 | 实现了 prompt 没明说、但显著提升体验的功能或细节，包括：识别隐性需求、识别 DP（设坑点）、补充常识级附加功能（撤销 / 复制 / 快捷键 / README 等）、做出 rubric 之外让人“哇”的点 | 操作完产物后能可数地说出“它多做了哪几件好事”；与其他模型对比有可见差异 |

### Pros · 多轮收敛与协作

| tag | 定义 | 判定信号 |
| --- | --- | --- |
| One-Shot 通过 | 第 1 轮就完整交付，不需要任何修正 | 直接看是否首轮就 OK |
| 精准定位修复 | 多轮 debug 时能直接找到根因，最小改动修复 | 看每一轮的修改范围 |
| 主动澄清 | 模糊需求型题目里主动提出关键澄清问题 | 看模型在第 1 轮是否反问而不是硬猜 |
| 保留上下文 | 多轮交互时记得之前的约定（命名、技术栈、风格） | 多轮后看代码风格是否一致 |

## Cons

### Cons · 功能缺陷

| tag | 定义 | 判定信号 |
| --- | --- | --- |
| 核心功能缺失 | 关键功能直接没做 | 对照 prompt 显性要求逐条核 |
| 行为不正确 | 功能在但行为不对（拖拽失灵、计算错误、状态不同步、点了无响应等） | 实际操作复现 |
| 边界处理缺失 | 空输入 / 极值 / 异常输入直接崩或行为离谱 | 故意输入畸形数据 |
| 状态不持久 | 刷新 / 切换 tab 后状态丢失（prompt 暗示需要持久化时） | 刷一次页面看 |

### Cons · 运行问题

| tag | 定义 | 判定信号 |
| --- | --- | --- |
| 加载缓慢 / 资源臃肿 | 页面初次加载明显慢，或反复加载相同资源 | 肉眼判断“打开太久才出内容”；可选开 Network 看请求数 / 总 size |
| 操作卡顿 | 普通操作（拖拽、点击、滚动）下肉眼可感知的卡顿 / 掉帧 | 实际操作若干次凭手感判断；不强制开 DevTools Performance |
| 使用报错 | 使用过程中 Console 持续刷红字，或 alert / 错误弹窗反复弹出 | 打开 Console 一眼可见，或操作时频繁被错误提示打断 |

### Cons · 视觉与体验缺陷

| tag | 定义 | 判定信号 |
| --- | --- | --- |
| 审美模板化 | 直接用 Bootstrap / Tailwind 默认配色或常见模板皮，无品牌感、无主题贴合 | 看一眼就有“这就是套个模板”的感觉 |
| 布局错乱 | 元素重叠、溢出窗口、文字截断 | 标准 1920×1080 屏幕一眼可见 |
| 配色冲突 | 色彩搭配生硬，前景背景对比不足 | 关键文字读不清楚 |
| 动效失控 | 动画过度 / 卡顿 / 在错误时机触发 | 观察至少一次完整交互 |
| 窗口自适应失败 | 拉窗口或切换设备尺寸后排版炸 | 试 375px / 768px / 1920px 三档 |

### Cons · 需求遵循缺陷

| tag | 定义 | 判定信号 |
| --- | --- | --- |
| 偏离主题 | 整体方向跑偏，做了别的东西 / 违反硬性限制 / 不顾 prompt 自己加一堆没要求的东西 | 看产物 30 秒就能识别；对照 prompt 看是否大方向一致 |

### Cons · 多轮收敛缺陷

| tag | 定义 | 判定信号 |
| --- | --- | --- |
| 难以 debug | 多轮调试中无法定向修复问题，指出问题后模型反复改不对、反复给错误方案 | 看是否在 5 轮上限内仍未解决同一个 bug |
| 拆东补西 | 修一个 bug 换出另一个 bug | 多轮对比看 |
| 遗忘上下文 | 后面轮次忘了前面定的约定（命名、技术栈、风格） | 看是否在多轮间漂移 |
| 主动放弃 | 直接说“这个我做不到”或胡乱给个 placeholder | 看是否中途交差 |

## Stylistic Fingerprints

### Stylistic Fingerprints · 技术栈偏好

| tag | 定义 | 判定信号 |
| --- | --- | --- |
| 偏好 Three.js | 3D 题倾向用 Three.js 而非纯 Canvas | 复杂 3D：正面；简单 2D 也用：负面（杀鸡用牛刀） |
| 偏好纯 Canvas | 即使有 Three.js 选项也用 Canvas 实现 3D | 轻量游戏：正面；复杂 3D：负面 |
| 偏好现代框架 | 不指定技术栈时默认套 React / Vue / Svelte 等现代框架 + Tailwind / UnoCSS 等 | 现代化场景：正面；轻量交付：负面 |
| 偏好原生 JS | 不引入框架直接写 vanilla（含纯 HTML / CSS / JS） | 单文件交付：正面；复杂应用：负面 |
| 重度依赖 CDN | 一上来就引 jQuery / Bootstrap / Three.js CDN | 演示：正面；离线场景：负面 |

### Stylistic Fingerprints · 架构与组织风格

| tag | 定义 | 判定信号 |
| --- | --- | --- |
| 单文件原教旨 | 不管多复杂都塞一个 HTML 里 | 单文件交付：正面；中大型：负面 |
| 过度拆分 | 简单需求拆成 N 个文件 N 层目录 | 大项目：正面；MVP：负面 |

### Stylistic Fingerprints · UI 风格指纹

| tag | 定义 | 判定信号 |
| --- | --- | --- |
| 偏好质感丰富 | 大量阴影 / 浮起卡片 / 毛玻璃 / 渐变层叠等“有材质感”的视觉处理（涵盖原 Material Design / Glassmorphism） | 后台系统 / 仪表盘：正面；轻量工具 / 文字密集页：负面 |
| 偏好极简留白 | 大量留白 + 单一强调色 + 细字重 + 极少装饰元素（涵盖原 Apple HIG / 极简主义） | 内容展示 / 高端工具：正面；游戏 / 3D 仿真 / 复杂应用：负面 |
| 偏好炫技氛围 | 偏爱霓虹色 / glow 阴影 / 大渐变 / 粒子动效等“视觉冲击”型处理（涵盖原 霓虹/赛博偏好） | 游戏 / cyber 主题：正面；专业工具 / 严肃场景：负面 |

### Stylistic Fingerprints · 起手式与流程习惯

| tag | 定义 | 判定信号 |
| --- | --- | --- |
| 先列计划 | 第 1 轮先输出 plan / 拆 step，再写代码 | 复杂题：正面；简单题：啰嗦 |
| 直接动手 | 拿到 prompt 直接写代码不寒暄 | 简单题：正面；复杂题：可能漏需求 |

### Stylistic Fingerprints · 多轮交互习惯

| tag | 定义 | 判定信号 |
| --- | --- | --- |
| 偏全量重写 | 每次修改重写整个文件 | 小文件：正面；大文件：低效 |
| 偏 patch 修改 | 每次只给 diff / patch | 大文件：正面；改动大：晦涩 |
| 自检后再交付 | 写完后自己描述测试过程 | 复杂题：正面；简单题：啰嗦 |
| 等用户喂报错 | 不主动跑代码，等用户贴报错才修 | 谨慎风格：可接受；高效场景：负面 |
