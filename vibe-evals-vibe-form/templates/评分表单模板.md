# {题目名}-评分表单

> 依据：Coding Agent 人评标准 V 2.1 ｜ 每维度 0–4，步长 0.5 ｜ 机器预填分数（G1/G2/S1 公式分、G3 与总体印象建议分）均标注【待人工确认】
> 材料：{反馈报告.md、rubrics-{X}.json、题包路径}
> **维度分来源（2026-09-23 定稿）**：**G1 指令与遵循 / G2 功能完整性 / S1 视觉审美 = 公式分**（`通过率 k/n × 4`，向下取整到 0.5 的倍数，由 `formula_dimension_scores.py` 计算；表单写 `【公式分 {x}，待人工确认】（k/n）`，不再使用 0–4 勾选栏，改为列 k/n 与依据条目）；**G3 任务收敛效率 = 建议分**（按 debug 轮数判，标 `【建议 {x}，待人工确认】`）；**总体印象分 = 有据可依的主观建议分**（不套公式，与排序严格一致）。

---

## 〇、题目级共享节

> 口吻规范（见 SKILL.md「文字口吻与真实性」）：排序理由、洞察、难点思路、机器证据、Pros/Cons/Style 全部先读材料再写，短句事实结论，禁止模板腔/AI话；洞察 = 跨模型看出来的共性问题或规律，不是逐模型复述。

### 模型排序

| 档位 | 模型 | 总体印象分 |
|------|------|-----------|
| 1 | **{X}** | 【建议 {x}，待人工确认】 |

<!-- 同档并列示例：| 1 | {X}、{Y} | ...（同档位号表示并列，V2.1 3.1） -->

**排序表达式**：{A > B = C}（1 最好，同档并列；N=1 时写“{模型}（单模型，无同场比较/无排序）”）

**排序依据（2026-09-14 起硬约束）**：按**总体印象分**定档（3 分 = 完成所有基本要求、4 分须有不属于基本要求的特别亮点、低于 3 分须有扣分依据）；印象分相同走**排序三层法**——**第 1 层**总体印象分；**第 2 层**比多维标签分的整体分布（维度集合 = 适用且已评分的维度，含 R1 适用时；同一维度分高者占优，做一张“同分模型逐维对照”小表，占优维度多者在前）；**第 3 层**仍分不出就写 `X = Y（需人工定档）` 并列出 tie-breaker（还原度实测、可见缺陷数、错项性质），**不得用小数分差或个别像素值硬排**；人工裁定后回写一行“已裁定：判并列 / 定序为 X > Y”，并同步排序表达式、排序理由与反馈报告 Rank。

**同分模型逐维对照小表（印象分相同就必须做；N=1 时删除本表）**：

> 维度列 = **适用且已评分的维度**（R1 适用于模糊需求型题时加一列；A1 按现行政策恒为 N/A 时不列）。

| 模型 | {维度列 = 适用且已评分的维度，如 G1 / G2 / G3 / S1，R1 适用时加一列} | 占优维度数 | 第二层结论 |
|------|--------------------------------|-----------|-----------|
| {X} | {各维度分} | {n} | {X 在前 / 分不出} |
| {Y} | {各维度分} | {n} | {Y 在前 / 分不出} |

**待人工确认清单**（统一放在**文件末尾**，随文滚动更新；排序节只写表达式，不重复清单）：档位与总体印象建议分、G1/G2/S1 公式分与 G3 建议分、并列是否需要人工定档、Vibe 标签勾选、是否并入历史总表。

**排序理由**（写可观察事实；**篇幅以 SKILL.md「排序理由与洞察速记风格」为硬上限：每模型 1 句、≤ 55 字，全节 ≤ 12 行或 ≤ 500 字**；"分水岭 1-3 句"是旧口径，不作为句数依据）：

{...}

### 洞察

- **最大模型差异**：{...}
- **能力边界 Insight**：{...}

### 难点和思路

{...}

---

## 一、模型 {X}

### 总体印象

综合这次交付给出的整体主观印象分。不是各维度的算术平均，允许与分维度结果不一致——如果不一致，请在文字反馈里说明原因。

{与排序一致；与均分不一致时说明理由}

【建议 {x}，待人工确认】　[ ] 0　[ ] 0.5　[ ] 1　[ ] 1.5　[ ] 2　[ ] 2.5　[ ] 3　[ ] 3.5　[ ] 4

### G1 · 指令与约束遵循

**评分标准**：是否准确落实显性指令、硬性限制和复杂组合约束，不擅自改变任务目标。

**机器证据**：{Explicit 类 rubric 得分（来自 rubrics-{X}.json）+ 违背/遗漏条目}

【公式分 {x}，待人工确认】（k/n = {k}/{n}，见《维度分-公式.md》，指令遵循维度公式分）

### G2 · 功能交付完整性

**评分标准**：代码是否真正可运行，核心链路、关键功能和必要边界是否完成。

**机器证据**：{功能类 rubric 得分（来自 rubrics-{X}.json）+ 4W1H 问题引用（来自反馈报告.md）}

【公式分 {x}，待人工确认】（k/n = {k}/{n}，见《维度分-公式.md》，功能完整性维度公式分）

### G3 · 任务收敛效率

**评分标准**：在任务目标和当前阶段要求已经明确后，模型能否通过实现、验证和修正，使任务高效、稳定地达到可验收状态。重点观察首轮实现基础、每轮有效进展、根因定位、重复犯错、修改范围、回归和最终收敛；轮次仅作参考。

**机器证据**：{对话记录修复轨迹 + debug 轮数（来自对话记录；上限：多轮 3 次/单轮 5 次）}

【建议 {x}，待人工确认】　[ ] 0　[ ] 0.5　[ ] 1　[ ] 1.5　[ ] 2　[ ] 2.5　[ ] 3　[ ] 3.5　[ ] 4

### ▾ 其他维度（按题目类型判定适用性，不适用标 N/A + 依据）

> 任一门控维度判为不适用时，把该节评分行整体替换为 `N/A【不适用：{依据}】`，不得保留数字建议值或 0–4 勾选栏。

#### S1 · 视觉审美（{适用/不适用}：{依据}）

**评分标准**：前端界面的布局、层级、配色、风格一致性，以及与参考图或主题的匹配程度。

**机器证据**：{图形与媒体类 rubric 得分（来自 rubrics-{X}.json）+ 记录.txt 排版意见}

【公式分 {x}，待人工确认】（k/n = {k}/{n}，见《维度分-公式.md》，视觉审美维度公式分）

#### A1 · 架构合理性（{适用/不适用}：{依据}）

**评分标准**：模块职责、代码边界、接口与数据契约是否清晰，修改和扩展成本是否可控。

**机器证据**：{模块结构证据（来自差异总表/反馈报告，或经 Codex 子智能体取证）；按 V2.1 目前无后端题统一标 N/A}

【建议 {x}，待人工确认】　[ ] 0　[ ] 0.5　[ ] 1　[ ] 1.5　[ ] 2　[ ] 2.5　[ ] 3　[ ] 3.5　[ ] 4

#### R1 · 需求理解与澄清（{适用/不适用}：{依据}）

**评分标准**：在开始实施前，模型能否准确理解用户真实意图，识别影响方案和验收结果的关键歧义，并通过必要提问、明确假设或提供选项，使需求达到可执行状态。

**机器证据**：{首轮对话提问质量（来自对话记录）；未提问标 N/A}

【建议 {x}，待人工确认】　[ ] 0　[ ] 0.5　[ ] 1　[ ] 1.5　[ ] 2　[ ] 2.5　[ ] 3　[ ] 3.5　[ ] 4

### 文字反馈（维度打分依据必须写在这里）

> Pros/Cons 不限定条数，没有真亮点可写“无”。Pros、Cons 分别按单一合法维度合并，同一块内同一标签只出现一次；禁止混合维度标签。每个出现的维度标签在 Pros 与 Cons 合集中只说明一次分数：只在 Pros 出现就由 Pros 写分，只在 Cons 出现就由 Cons 写分，两块都有时只由 Cons 写分。总体印象低于 3.0 时 Pros 默认写“无”，只有材料证明明显超过题目预期或人工明确认可的突出亮点才例外（**含“某维度分 ≥3.5 且确有亮点、由 Pros 承担”这一优先情形**）。反馈只写材料可证实的具体对象、动作或状态、结果。
> **3 分线（硬约束）**：3 分 = 完成所有基本要求（验收线），4 分必须有不属于基本要求的特别亮点，低于 3 分必须有扣分依据。**总体印象 ≥3.0 但材料只有“完成基本要求”类事实时，Pros 同样写“无”**（“跟参考图接近 / 产物能跑 / 完成度高”都不是加分理由）；任何 3.5 / 4 的总体印象分或维度分，都要在 Pros 里给出不属于基本要求的亮点事实。

#### 👍 亮点 Pros

```
{预填亮点条目，机器建议}
```

#### 👎 缺陷 Cons

```
{预填缺陷条目，机器建议——与各维度低分对应}
```

#### 🎨 风格 / 其他 Style

```
{预填风格指纹，机器建议}
```

### Vibe 标签（候选勾选：有必要的都勾，可多选；没有突出依据的不硬贴）

**正面亮点**：{从 V2.1标签库.md 预选：- [ ] 标签名（判定信号：{...}）}
**负面缺陷**：{同上}
**风格指纹**：{同上}

---

## 二、模型 {Y}

### 总体印象

综合这次交付给出的整体主观印象分。不是各维度的算术平均，允许与分维度结果不一致——如果不一致，请在文字反馈里说明原因。

{与排序一致；与均分不一致时说明理由}

【建议 {x}，待人工确认】　[ ] 0　[ ] 0.5　[ ] 1　[ ] 1.5　[ ] 2　[ ] 2.5　[ ] 3　[ ] 3.5　[ ] 4

### G1 · 指令与约束遵循

**评分标准**：是否准确落实显性指令、硬性限制和复杂组合约束，不擅自改变任务目标。

**机器证据**：{Explicit 类 rubric 得分（来自 rubrics-{Y}.json）+ 违背/遗漏条目}

【公式分 {x}，待人工确认】（k/n = {k}/{n}，见《维度分-公式.md》，指令遵循维度公式分）

### G2 · 功能交付完整性

**评分标准**：代码是否真正可运行，核心链路、关键功能和必要边界是否完成。

**机器证据**：{功能类 rubric 得分（来自 rubrics-{Y}.json）+ 4W1H 问题引用（来自反馈报告.md）}

【公式分 {x}，待人工确认】（k/n = {k}/{n}，见《维度分-公式.md》，功能完整性维度公式分）

### G3 · 任务收敛效率

**评分标准**：在任务目标和当前阶段要求已经明确后，模型能否通过实现、验证和修正，使任务高效、稳定地达到可验收状态。重点观察首轮实现基础、每轮有效进展、根因定位、重复犯错、修改范围、回归和最终收敛；轮次仅作参考。

**机器证据**：{对话记录修复轨迹 + debug 轮数（来自对话记录；上限：多轮 3 次/单轮 5 次）}

【建议 {x}，待人工确认】　[ ] 0　[ ] 0.5　[ ] 1　[ ] 1.5　[ ] 2　[ ] 2.5　[ ] 3　[ ] 3.5　[ ] 4

### ▾ 其他维度（按题目类型判定适用性，不适用标 N/A + 依据）

> 任一门控维度判为不适用时，把该节评分行整体替换为 `N/A【不适用：{依据}】`，不得保留数字建议值或 0–4 勾选栏。

#### S1 · 视觉审美（{适用/不适用}：{依据}）

**评分标准**：前端界面的布局、层级、配色、风格一致性，以及与参考图或主题的匹配程度。

**机器证据**：{图形与媒体类 rubric 得分（来自 rubrics-{Y}.json）+ 记录.txt 排版意见}

【公式分 {x}，待人工确认】（k/n = {k}/{n}，见《维度分-公式.md》，视觉审美维度公式分）

#### A1 · 架构合理性（{适用/不适用}：{依据}）

**评分标准**：模块职责、代码边界、接口与数据契约是否清晰，修改和扩展成本是否可控。

**机器证据**：{模块结构证据（来自差异总表/反馈报告，或经 Codex 子智能体取证）；按 V2.1 目前无后端题统一标 N/A}

【建议 {x}，待人工确认】　[ ] 0　[ ] 0.5　[ ] 1　[ ] 1.5　[ ] 2　[ ] 2.5　[ ] 3　[ ] 3.5　[ ] 4

#### R1 · 需求理解与澄清（{适用/不适用}：{依据}）

**评分标准**：在开始实施前，模型能否准确理解用户真实意图，识别影响方案和验收结果的关键歧义，并通过必要提问、明确假设或提供选项，使需求达到可执行状态。

**机器证据**：{首轮对话提问质量（来自对话记录）；未提问标 N/A}

【建议 {x}，待人工确认】　[ ] 0　[ ] 0.5　[ ] 1　[ ] 1.5　[ ] 2　[ ] 2.5　[ ] 3　[ ] 3.5　[ ] 4

### 文字反馈（维度打分依据必须写在这里）

> Pros/Cons 不限定条数，没有真亮点可写“无”。Pros、Cons 分别按单一合法维度合并，同一块内同一标签只出现一次；禁止混合维度标签。每个出现的维度标签在 Pros 与 Cons 合集中只说明一次分数：只在 Pros 出现就由 Pros 写分，只在 Cons 出现就由 Cons 写分，两块都有时只由 Cons 写分。总体印象低于 3.0 时 Pros 默认写“无”，只有材料证明明显超过题目预期或人工明确认可的突出亮点才例外（**含“某维度分 ≥3.5 且确有亮点、由 Pros 承担”这一优先情形**）。反馈只写材料可证实的具体对象、动作或状态、结果。
> **3 分线（硬约束）**：3 分 = 完成所有基本要求（验收线），4 分必须有不属于基本要求的特别亮点，低于 3 分必须有扣分依据。**总体印象 ≥3.0 但材料只有“完成基本要求”类事实时，Pros 同样写“无”**（“跟参考图接近 / 产物能跑 / 完成度高”都不是加分理由）；任何 3.5 / 4 的总体印象分或维度分，都要在 Pros 里给出不属于基本要求的亮点事实。

#### 👍 亮点 Pros

```
{预填亮点条目，机器建议}
```

#### 👎 缺陷 Cons

```
{预填缺陷条目，机器建议——与各维度低分对应}
```

#### 🎨 风格 / 其他 Style

```
{预填风格指纹，机器建议}
```

### Vibe 标签（候选勾选：有必要的都勾，可多选；没有突出依据的不硬贴）

**正面亮点**：{从 V2.1标签库.md 预选：- [ ] 标签名（判定信号：{...}）}
**负面缺陷**：{同上}
**风格指纹**：{同上}

---

> 每模型一节，重复上述结构；模型数 > 2 时按「三、模型 {Z} …」继续追加。

---

## 待人工确认清单（每个表单都必须有，统一放文件末尾）

- [ ] 档位与总体印象建议分（{模型列表}）
- [ ] 印象分相同的模型是否需要人工定档（第三层结论 + 已列出的 tie-breaker；人工裁定后回写“已裁定：判并列 / 定序为 X > Y”）
- [ ] 各适用维度分数（G1/G2/S1 为公式分、G3 为建议分；R1 适用于模糊需求型题时一并登记）
- [ ] Vibe 标签勾选
- [ ] 是否并入历史总表（如 `PS-0826\Vibe Evals评测材料\PS-0826-评分表单.md`）
